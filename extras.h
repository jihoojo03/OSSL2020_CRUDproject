#ifndef _EXTRAS_H_
#define _EXTRAS_H_

#include "record.h"

// function prototypes
void defrag_menu(Record[]);
void sort_menu(Record[]);
void make_setmenu(Record[]);
void today_sales(Record[]);

#endif
